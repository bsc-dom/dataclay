language: python
python:
        - "3.6" #current default Python on travis CI
        - "3.7"
install:
        - pip install -r requirements.txt
script:
        - pytest
notifications:
  email:
    recipients:
    - support-dataclay@bsc.es
    on_success: always
    on_failure: always
