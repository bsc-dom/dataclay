class RegistrationInfo(object):
    def __init__(self, object_id, class_id, store_session_id, dataset_name, alias):
        self.object_id = object_id
        self.class_id = class_id
        self.store_session_id = store_session_id
        self.dataset_name = dataset_name
        self.alias = alias
